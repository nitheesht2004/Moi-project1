{
    "name": "moi-panam-manager",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": "24.x"
    },
    "scripts": {
        "build": "cd frontend && npm install && npm run build"
    }
}